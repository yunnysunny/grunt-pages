extends ../../layouts/base
block content
  ul
    each post in posts
      li
        br
        a(href = "/#{post.url}") #{post.title}
    each page, pageNumber in pages
      if page.currentPage
        span #{pageNumber}
      else
        a(href = "#{page.url}") #{pageNumber}
