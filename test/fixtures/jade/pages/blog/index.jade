extends ../../layouts/base
block content
  ul
    each post in posts
      li
        br
        a(href = "/#{post.url}") #{post.title}
    - for (i = 1; i < numPages; i++) {
    - if (i === numPages)
        if pageNumber
          span #{pageNumber}
      else
        a(href = "#{baseUrl}/page/#{i}/") #{i}
    - }
